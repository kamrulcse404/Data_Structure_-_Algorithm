#include <stdio.h>

int main(){

    int number;
    printf("Input a number for Cube : ");

    scanf("%d", &number);

    printf("Cube of the number is: %d", number * number * number);


    return 0;
}